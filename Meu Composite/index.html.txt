<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Nanda | Modelo & UGC Creator</title>
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600&family=Playfair+Display:ital,wght@0,400;0,600;1,400&display=swap" rel="stylesheet">

    <style>
        /* --- ESTILOS (CSS) --- */
        :root {
            --bg-color: #FDFBF7;
            --text-dark: #1A1814;
            --accent: #C27654;
            --font-head: 'Playfair Display', serif;
            --font-body: 'Inter', sans-serif;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }
        html { scroll-behavior: smooth; }
        body { 
            font-family: var(--font-body); 
            background-color: var(--text-dark);
            color: var(--text-dark); 
            -webkit-font-smoothing: antialiased;
        }

        .app-container {
            max-width: 480px;
            margin: 0 auto;
            background-color: var(--bg-color);
            position: relative;
            min-height: 100vh;
            overflow-x: hidden;
            box-shadow: 0 0 40px rgba(0,0,0,0.5);
            padding-bottom: 80px; /* Espaço para o botão não cobrir o conteúdo final */
        }

        /* --- HERO SECTION --- */
        .hero {
            height: 100vh;
            position: relative;
            display: flex;
            align-items: flex-end;
            padding: 2rem 1.5rem 6rem;
            color: #ffffff;
        }
        
        /* ATENÇÃO: Verifique se a extensão do seu vídeo é .mp4 mesmo */
        .hero-media {
            position: absolute;
            top: 0; left: 0;
            width: 100%; height: 100%;
            object-fit: cover;
            z-index: 1;
        }
        
        .hero-overlay {
            position: absolute;
            top: 0; left: 0;
            width: 100%; height: 100%;
            background: linear-gradient(to top, rgba(26, 24, 20, 0.9) 0%, rgba(26, 24, 20, 0.2) 50%, rgba(0,0,0,0.1) 100%);
            z-index: 2;
        }

        .hero-content {
            z-index: 3;
            width: 100%;
            text-align: left;
        }

        .hero h1 {
            font-family: var(--font-head);
            font-size: 3.2rem;
            line-height: 1;
            margin-bottom: 0.5rem;
            font-weight: 600;
        }

        .hero p {
            font-size: 1rem;
            font-weight: 300;
            opacity: 0.9;
            line-height: 1.5;
            max-width: 90%;
        }

        /* --- GALERIA DINÂMICA --- */
        .section-title {
            font-family: var(--font-head);
            font-size: 2rem;
            text-align: center;
            margin: 3rem 0 1.5rem;
            color: var(--text-dark);
        }

        .gallery {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 12px;
            padding: 0 16px;
        }

        .gallery-item {
            border-radius: 16px;
            overflow: hidden;
            position: relative;
            aspect-ratio: 4/5;
            background-color: #EFEBE4;
        }

        .gallery-item.large { grid-column: span 2; aspect-ratio: 16/9; }
        .gallery-item.tall { grid-column: span 2; aspect-ratio: 9/16; }

        /* ATENÇÃO: Verifique se a extensão das suas fotos é .jpg mesmo */
        .gallery-item img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.5s ease;
        }

        /* --- PERFIL E MEDIDAS --- */
        .profile {
            padding: 4rem 2rem 2rem;
            text-align: center;
        }

        .profile p {
            font-size: 1rem;
            line-height: 1.6;
            margin-bottom: 2rem;
            color: #4A453E;
        }

        .measurements {
            display: flex;
            justify-content: center;
            gap: 3rem;
            background: #ffffff;
            padding: 1.5rem;
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.03);
        }

        .measure-item {
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .measure-value {
            font-family: var(--font-head);
            font-size: 2rem;
            color: var(--accent);
            font-weight: 600;
        }

        .measure-label {
            font-size: 0.75rem;
            text-transform: uppercase;
            letter-spacing: 2px;
            color: #8C857B;
            margin-top: 0.3rem;
        }

        /* --- BOTÃO CTA FLUTUANTE --- */
        .cta-btn {
            position: fixed;
            bottom: 24px;
            left: 50%;
            transform: translateX(-50%);
            background-color: var(--text-dark);
            color: #ffffff;
            padding: 1rem 2rem;
            border-radius: 50px;
            text-decoration: none;
            font-weight: 600;
            font-size: 1rem;
            z-index: 100;
            width: calc(100% - 32px);
            max-width: 448px;
            text-align: center;
            box-shadow: 0 15px 25px rgba(26, 24, 20, 0.3);
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 10px;
            transition: transform 0.2s, background-color 0.2s;
        }

        .cta-btn:active {
            transform: translateX(-50%) scale(0.96);
            background-color: var(--accent);
        }

        /* --- ANIMAÇÕES --- */
        .fade-in {
            opacity: 0;
            transform: translateY(30px);
            transition: opacity 0.8s ease-out, transform 0.8s ease-out;
        }
        .fade-in.visible {
            opacity: 1;
            transform: translateY(0);
        }
    </style>
</head>
<body>

<div class="app-container">

    <header class="hero">
        <video class="hero-media" src="v09044g40000d140alfog65kkt57575g.mp4" autoplay loop muted playsinline></video>
        
        <div class="hero-overlay"></div>
        
        <div class="hero-content fade-in">
            <h1>Nanda</h1>
            <p>Modelo & UGC Creator.<br>Trazendo brasilidade e sofisticação para marcas com propósito.</p>
        </div>
    </header>

    <section class="gallery-section">
        <h2 class="section-title fade-in">Portfólio</h2>
        <div class="gallery">
            
            <div class="gallery-item tall fade-in">
                <img src="IMG_2326.jpg" alt="Foto Portfólio 1">
            </div>

            <div class="gallery-item fade-in">
                <img src="IMG_2317.jpg" alt="Foto Portfólio 2">
            </div>

            <div class="gallery-item fade-in">
                <img src="IMG_2280 (1).jpg" alt="Foto Portfólio 3">
            </div>

            <div class="gallery-item large fade-in">
                <img src="IMG_5065 (1).jpg" alt="Foto Portfólio 4">
            </div>

            <div class="gallery-item fade-in">
                <img src="IMG_5061 (1).jpg" alt="Foto Portfólio 5">
            </div>

             <div class="gallery-item fade-in">
                <img src="IMG_5062.jpg" alt="Foto Portfólio 6">
            </div>

            <div class="gallery-item fade-in">
                <img src="IMG_5340.jpg" alt="Foto Portfólio 7">
            </div>

        </div>
    </section>

    <section class="profile fade-in">
        <h2 class="section-title" style="margin-top: 0;">Medidas</h2>
        <p>Pronta para criar visuais autênticos que conectam a sua marca diretamente com o público.</p>
        
        <div class="measurements">
            <div class="measure-item">
                <span class="measure-value">1.71</span>
                <span class="measure-label">Altura (m)</span>
            </div>
            <div class="measure-item">
                <span class="measure-value">36/38</span>
                <span class="measure-label">Manequim</span>
            </div>
        </div>
    </section>

    <a href="https://wa.me/5571992471571?text=Olá%20Nanda!%20Vimos%20seu%20composite%20e%20queremos%20criar%20juntos." class="cta-btn" target="_blank">
        Vamos criar juntos? ✦
    </a>

</div>

<script>
    document.addEventListener("DOMContentLoaded", () => {
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if(entry.isIntersecting) {
                    entry.target.classList.add('visible');
                }
            });
        }, { threshold: 0.1 });
        
        document.querySelectorAll('.fade-in').forEach(el => observer.observe(el));
    });
</script>

</body>
</html>